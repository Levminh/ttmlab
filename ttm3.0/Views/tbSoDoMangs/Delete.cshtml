@model ttm3._0.Models.tbSoDoMang

@{
    ViewBag.Title = "Xóa sơ đồ mạng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Content/themes/base/jquery-ui.min.css" rel="stylesheet" />

<div class="ttm-page">
    <div class="ttm-card">
        <div class="ttm-header">
            <div class="ttm-header-left">
                <div class="ttm-icon"><i class="fas fa-project-diagram"></i></div>
                <div>
                    <h1 class="ttm-title">@ViewBag.Title</h1>
                    <p class="ttm-subtitle">Bạn có chắc chắn muốn xóa?</p>
                </div>
            </div>
        </div>
        <div class="ttm-body">
            <dl class="dl-horizontal">
                <dt>@Html.DisplayNameFor(model => model.Ten)</dt>
                <dd>@Html.DisplayFor(model => model.Ten)</dd>
                <dt>@Html.DisplayNameFor(model => model.UUID)</dt>
                <dd>@Html.DisplayFor(model => model.UUID)</dd>
            </dl>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="form-actions no-color" style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-top: 1rem;">
                    <button type="submit" class="ttm-btn ttm-btn-danger">
                        <i class="fas fa-trash-alt"></i>
                        <span>Xóa</span>
                    </button>
                    <a href="@Url.Action("Index", "tbSoDoMangs")" class="ttm-btn ttm-btn-secondary">
                        <i class="fas fa-arrow-left"></i>
                        <span>Quay lại</span>
                    </a>
                </div>
            }
        </div>
    </div>
</div>
